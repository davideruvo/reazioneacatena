const CONFIG = { fetchDelay: 0 };

const SITE = {
  title: "Site Title Site Title Site Title",
};

const STATUS = {
  loading: "LOADING",
  complete: "COMPLETE",
  error: "ERROR",
};

export { CONFIG, SITE, STATUS };
